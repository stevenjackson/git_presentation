```
  if(!null) {
    if(complicated && impossible ? neverHappens() : doesntMatter()) {
       if(complicated) {
         try {
           if(variableWeDontAppearToUseAnywhere == null) {
             codeThatLooksCorrectButCanNeverBeReachedInTheKnownUniverse()
           } else if(unrelatedSideEffect) {
             codeThatLooksReallyStrangeButMightBeTheProblem()
           } else {
             if(unrelatedSideEffect == 1) DoNotPassGo()
             FooYou!()

             TheOnlyReasonYouMightCallThisMonstrosity()
           }
         } catch(Exception ex) {
             TheErrorYouAreTryingToDebug();
         }
       } else {
         barBazTheBillyBangBang()
       }
    }
  }
```
